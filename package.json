{
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "rimraf": "^6.1.2",
    "tailwindcss": "^3.4.1"
  },
  "scripts": {
    "build": "npx tailwindcss -i ./src/css/input.css -o ./dist/css/output.css --minify",
    "prebuild": "rimraf dist && xcopy src dist /E /I /Y"
  }
}
